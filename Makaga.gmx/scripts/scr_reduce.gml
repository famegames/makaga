//  Usage:
//      speed=reduce(speed);
//
//  Arguments:
//      a number  -real
//
//  Returns:
//      the number, but reduced so it is closer to 0.

return max(abs(argument0)-1,0)*sign(argument0);

